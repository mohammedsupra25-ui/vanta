# Sanity CMS — set your project ID after running `cd studio && sanity init`
# Leave empty to use built-in mock data (works out of the box)
VITE_SANITY_PROJECT_ID=dsif98nr
VITE_SANITY_DATASET=production
